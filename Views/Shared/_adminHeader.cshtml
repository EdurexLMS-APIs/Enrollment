<ul class="navbar-nav">
    <li class="nav-item" style="color: #058383 !important "><i data-feather="user"></i><span class="font-weight-medium ml-1 mr-1 d-none d-md-inline-block">Hi, @CPEA.Utilities.GeneralClass.FullName</span></li>
    
</ul>





@*<nav class="navbar top-navbar">
    <div class="container">
        <div class="navbar-content">
            
			<ul class="navbar-nav">
				@if (CPEA.Utilities.GeneralClass.email != "")
				{
					<li class="dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="profileDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color: #058383 !important ">
                            <i data-feather="user"></i><span class="font-weight-medium ml-1 mr-1 d-none d-md-inline-block">Hi, @CPEA.Utilities.GeneralClass.FullName</span>
                        </a>
						<div class="dropdown-menu" aria-labelledby="profileDropdown">							
							<div class="dropdown-body">
                                <ul class="profile-nav p-0 pt-3">
                                    <li class="nav-item">
                                        <a asp-controller="Admin" asp-action="Login" class="nav-link">
                                            <i data-feather="log-out"></i>
                                            <span>Log Out</span>
                                        </a>
                                    </li>
                                </ul>
							</div>
						</div>
					</li> 
				 }
			</ul>
        </div>
    </div>
</nav>*@

<script>

    function Reset(email)
    {
        if (email != null)
        {
            if (confirm("Do you want to reset your password?"))
            {
                window.location.href = '@Url.Action("ResetPassword", "Admin")/' + email;
            }
        }
    }
</script>