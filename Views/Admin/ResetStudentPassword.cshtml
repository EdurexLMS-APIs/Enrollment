<!-- Modal -->

@model CPEA.Utilities.ViewModel.RegisteredStudentsVM
<div class="modal fade" id="resetStudentModal" tabindex="-1" role="dialog" aria-labelledby="resetStudentModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="resetStudentModalLabel">Reset Student Password</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form asp-action="ResetStudentPassword">
                    <div class="form-group">
                        <label for="studentNumber" class="col-form-label">Student Number :</label>
                        <input type="text" class="form-control" id="studentNumberR" readonly>
                    </div>
                    <div class="form-group">
                        <label for="studentEmail" class="col-form-label">Student Email :</label>
                        <input type="text" class="form-control" id="studentEmailR" readonly asp-for="@Model.ResetUserPasswordDTO.userEmail">
                    </div>
                    <div class="form-group">
                        <label for="studentName" class="col-form-label">Student Name :</label>
                        <input type="text" class="form-control" id="studentNameR" readonly>
                    </div>
                    <div class="form-group">
                        <label for="newPassword" class="col-form-label">New Password :</label>
                        <div class="search">
                            <span class="form-element">
                                <span class="fa fa-eye btn btn-default" id="toggleP" style="display:none;" onclick="clicktoggleP()"></span>
                                <input class="form-control" type="password" asp-for="@Model.ResetUserPasswordDTO.newPassword" id="password" required placeholder="Password" onkeyup="displayIcon()">
                            </span>
                        </div>
                        @*<input type="password" class="form-control" id="newPassword" asp-for="@Model.ResetUserPasswordDTO.newPassword">*@
                    </div>
                    <div class="form-group">
                        <label for="pin" class="col-form-label">Your PIN:</label>
                        <input type="password" class="form-control" id="pin" asp-for="@Model.ResetUserPasswordDTO.PIN">
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                        <button type="submit" class="btn btn-primary" style="margin-right: 10px; background-color: #058383 !important">Reset</button>
                    </div>
                </form>
            </div>

        </div>
    </div>
</div>
<script>
    function displayIcon() {

        var value = document.getElementById('password').value;
        if (value.length > 0) {
            $("#toggleP").css("display", "block");
        }
        else {

            $("#toggleP").css("display", "none");
        }
    }

    function clicktoggleP() {

        var type = $("#password").attr('type');

        if (type == "password") {
            $("#password").attr('type', 'text');
        }
        else {
            $("#password").attr('type', 'password');

        }
    }
</script>